---
description: Graphify scopes (root + theme RSL + project). Lookup OK; refresh only via skills/user.
alwaysApply: true
---

# Graphify (ITD)

Three graph scopes:

| Scope | Graph path | Refresh |
|-------|------------|---------|
| Root (repo) | `graphify-out/graph.json` | `npm run graphify:refresh` / skill **graphify-root** |
| Theme RSL (legado) | `docs/[titulo-breve]/graphify-out/graph.json` | `npm run graphify:theme -- <slug>` / skill **graphify-theme** |
| Proyecto académico | `docs/content/[FOLDER]/graphify-out/graph.json` | `pnpm graphify:project -- <FOLDER>` / skill **graphify-project** |

## Lookup (default — agents MUST prefer graph before wide Read)

Root:

```bash
graphify query "<question>"
graphify path "A" "B"
graphify explain "<concept>"
```

Theme RSL (require `--graph`):

```bash
graphify query "<question>" --graph docs/[titulo-breve]/graphify-out/graph.json
```

Proyecto (require `--graph`):

```bash
graphify query "<question>" --graph docs/content/[FOLDER]/graphify-out/graph.json
```

**Skills que operan sobre un proyecto** (`docs/content/<FOLDER>/`: bibliography-*, init-project-mvp, redacción/citas del proyecto, etc.) **deben** consultar primero el grafo del proyecto. Objetivo: menos tokens (no volcar PDFs/MD) y búsqueda localizable (nodo + `src` + página/`loc`). Responder con lo que Graphify devolvió; solo Read puntual de líneas si hace falta citar.

Prefer **project** graph for `profile.md`, `structure`/`modelo`, course notes, `bibliography/auto` + `bibliography/docs`. Prefer theme graph for legado RSL (`informe`, SLRs, PDFs, `topic.md`). Prefer root for skills, README, cross-project layout.

If `graphify-out/wiki/index.md` (or project/theme wiki) exists, navigate it before dumping large files.

Si el grafo del proyecto no existe → pedir **graphify-project** (no inventar; no sustituir con grep masivo del corpus).

## Índice del informe (proyectos)

1. `docs/content/[FOLDER]/structure.md` local si existe con contenido útil → **override**.
2. Si no → path en `config.modelo` (p. ej. `common/structure/model1.md`).

## Refresh — human / skill owned

**Never** run `npm run graphify:refresh`, `npm run graphify:theme`, `pnpm graphify:project`, or `graphify update` from casual coding unless the user **explicitly** invokes **graphify-root** / **graphify-theme** / **graphify-project** (or asks to refresh).
